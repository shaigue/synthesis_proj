"""TODO: add type annotations to improve the synthesizer"""


def add(x: int, y: int) -> int:
    return x + y


def sub(x: int, y: int) -> int:
    return x - y


def mul(x: int, y: int) -> int:
    return x * y


def eq(x: int, y: int) -> bool:
    return x == y


def lt(x: int, y: int) -> bool:
    return x < y
